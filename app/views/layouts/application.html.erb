<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Sportstore" %></title>

    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Sportstore">
    <meta name="mobile-web-app-capable" content="yes">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <!-- Bootstrap CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <!-- Bootstrap JS -->
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      defer
      integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
      crossorigin="anonymous"></script>
  </head>

  <body class="bg-light">

    <!-- NAVBAR -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
      <div class="container d-flex justify-content-between align-items-center">

        <!-- ЛОГО -->
        <%= link_to "SportStore", products_path,
              class: "navbar-brand fw-bold" %>

        <div class="d-flex gap-2 align-items-center">

          <!-- ➕ ДОДАТИ ТОВАР (тільки для залогінених) -->
          <% if user_signed_in? %>
            <%= link_to "➕ Додати товар",
                  new_product_path,
                  class: "btn btn-success" %>
          <% end %>

          <!-- 🛒 КОРЗИНА -->
          <%= link_to cart_path,
                class: "btn btn-light position-relative" do %>

            🛒 Корзина

            <% if session[:cart]&.any? %>
              <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">
                <%= session[:cart].values.sum %>
              </span>
            <% end %>

          <% end %>

          <!-- 👤 АВТОРИЗАЦІЯ -->
          <% if user_signed_in? %>
            <span class="text-light me-2">
              👤 <%= current_user.nickname %>
            </span>

            <%= link_to "✉ Зворотній звʼязок",
              new_feedback_path,
              class: "btn btn-outline-light" %>


            <%= button_to "Вийти",
                  destroy_user_session_path,
                  method: :delete,
                  class: "btn btn-outline-light" %>
          <% else %>
            <%= link_to "Увійти",
                  new_user_session_path,
                  class: "btn btn-outline-light" %>

            <%= link_to "Реєстрація",
                  new_user_registration_path,
                  class: "btn btn-outline-light" %>
          <% end %>

        </div>
      </div>
    </nav>

    <!-- КОНТЕНТ -->
    <main class="container mt-4">

      <% if notice %>
        <div class="alert alert-success"><%= notice %></div>
      <% end %>

      <% if alert %>
        <div class="alert alert-danger"><%= alert %></div>
      <% end %>

      <%= yield %>
    </main>

  </body>
</html>
